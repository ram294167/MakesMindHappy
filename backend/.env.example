APP_NAME=MindPath Career Mentor API
APP_VERSION=1.0.0
DEBUG=True

# PostgreSQL connection
DATABASE_URL=postgresql://postgres:yourpassword@localhost:5432/mindpath_db

# CORS — set to your Next.js URL in production
ALLOWED_ORIGINS=["http://localhost:3000","https://yourdomain.com"]

# WhatsApp number (with country code, no + or spaces)
WHATSAPP_NUMBER=919666889722

# Admin secret key — change in production
ADMIN_SECRET_KEY=change-this-to-a-random-secret-in-production
